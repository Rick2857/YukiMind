# 自动化

现有 `AutomationCapabilityRegistry` 注册：

- `emoji.send`：按 `emotion/intended_tone` 从当前作用域选择表情。
- `emoji.send_by_id`：发送创建任务时明确写入的可用表情 ID。

普通用户只能发送给创建者本人或任务创建时的当前群；超级管理员仍受创建时委托快照与执行时当前权限交集约束。网页、历史、模型或前序步骤输出不能动态进入 `emoji_id`。执行时重新检查系统开关、资产状态、作用域和文件，成功后写聊天账本、自动化步骤统计和表情使用次数。自动化不能采用、删除、封禁表情。
