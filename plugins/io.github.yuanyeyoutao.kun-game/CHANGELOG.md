# Changelog

## Unreleased

- 修正磨炼失败提示，使显示增重与实际体重变化一致。
- 为攻击、吞噬、强袭 BOSS 使用独立伤害规则；强袭会校验并消耗神器与节操。
- 阵亡记录在持久化状态中仅保留最近 20 条，并自动压缩已有超长记录。

## 1.0.0 - 2026-08-02

- 将完整养鲲玩法适配为 Yuki Plugin API v1 的 `play` 与 `admin` 命令。
- 增加 Host 静态 `*` 直达绑定、可信提及 PVP、私聊/群聊状态隔离和上海时间换日。
- 将规则提取为无 I/O 的确定性引擎，以每作用域锁和整状态 CAS 单次提交。
- 修复负数与非有限数、重复处罚、展示重复、数星星答案、BOSS 排行和拍卖资产唯一性问题。
- 首版不提供 AstrBot `groups.json` 运行时迁移。
